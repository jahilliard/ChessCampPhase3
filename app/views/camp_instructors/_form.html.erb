<%= form_for(@camp_instructor) do |f| %>
  <% if @camp_instructor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@camp_instructor.errors.count, "error") %> prohibited this Camp Instructor from being saved:</h2>

      <ul>
      <% @camp_instructor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :camp_id %><br />
    <%= f.collection_select :camp_id, get_camp_options, prompt: "Select Camp" %>
  </div>
  <div class="field">
    <%= f.label :instructor_id %><br />
    <%= f.select :instructor_id, get_instructor_options, prompt: "Select Instructor" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
